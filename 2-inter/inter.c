//Fail - Unfinished

#include <unistd.h>

int		main(int argc, char **argv)
{
	// how do you store which characters have already been printed
	//or how do you remove the doubles from the first string
	if (argc == 3){

		int i = 0;
		int x = 0;
		while (argv[1][i])
		{

			if (argv[1][i] == )
			i++;
		}
	}
	write(1, "\n", 1);
}